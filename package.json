{
  "name": "fullstackopen-part-3",
  "version": "0.0.1",
  "description": "phonebook",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../fullstackOpen/part-2/thePhonebook && npm run build && cp -r build ../../../FullstackOpen-Part-3",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com-personal:AbenezerKitata/FullstackOpen-Part-3.git"
  },
  "keywords": [
    "fullstackOpen"
  ],
  "author": "AbenezerKitata",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
